import { ReportsAnalyticsComponent } from './reports-analytics.component';

describe('ReportsAnalyticsComponent', () => {
  it('should create', () => {
    const component = new ReportsAnalyticsComponent();
    expect(component).toBeTruthy();
  });
});
