import { AttendanceManagementComponent } from './attendance-management.component';

describe('AttendanceManagementComponent', () => {
  it('should create', () => {
    const component = new AttendanceManagementComponent();
    expect(component).toBeTruthy();
  });
});
