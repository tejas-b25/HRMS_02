import { NotificationsAnnouncementsComponent } from './notifications-announcements.component';

describe('NotificationsAnnouncementsComponent', () => {
  it('should create', () => {
    const component = new NotificationsAnnouncementsComponent();
    expect(component).toBeTruthy();
  });
});
